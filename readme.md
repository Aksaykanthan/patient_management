
create a python virtual environment

    python -m venv virt

activate the virtual environment

    source virt/bin/activate        --> Mac
    set virt/Scripts/activate       --> Windows. ** if it doesn't work, try using source instead of set

Install the required packages from requirement.txt file

    pip install -r requirements.txt

then, to start the flask server, execute the following command

    ./run

